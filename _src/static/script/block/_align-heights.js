// alignHeights('.cert-exam__card');

// $(window).resize(function() {
// 	alignHeights('.cert-exam__card');	
// })

// function alignHeights(elem) {
// 	if($(window).width() > 576 && $('.cert-exam__card').length > 1) {
// 		let maxHeight = 0;
// 		$(elem).each(function(){
// 			var thisH = $(this).height();
// 			if (thisH > maxHeight) { maxHeight = thisH; }
// 		});
	
// 		$(elem).height(maxHeight);
// 	}
// }